<div class="container">
  <div class="row">
    <div class="col-md-3">

  <div>


  <h3>User info</h3>
  <table class="table">
    <tbody>
      <tr>
      <p><%= image_tag @book_detail.user.get_profile_image(100,100) %></p>
      <tr>
      <th>name <%= @book_detail.user.name %></th>

      </tr>
      <tr>
      <th>introduction <%= @book_detail.user.introduction %></th>
      </tr>
      </tr>
      </tbody>
      </table>
       <%= link_to edit_user_path(@book_detail.user), class: "btn btn-outline-secondary btn-block4" do %>
                  <i class="fas fa-user-cog"></i>
                  <% end %>



</div>






<h1>New book</h1>
<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <div>
  <%= f.submit 'Create Book', class: 'btn btn-success' %>
<% end %>
</div>
</div>
    <div class="col-md-8 offset-md-1">
<h1>Book detail</h1>
  <table class="table">
    <tr>
      <th>Title</th>
      <th>Body</th>
    </tr>
    <tr>
      <td><%= image_tag @book_detail.user.get_profile_image(100,100) %><br>
        <%= link_to @book_detail.user.name, user_path(@book_detail.user) %>
      </td>
      <td><%= link_to @book_detail .title, book_path(@book_detail) %></td>
      <td><%= @book_detail.body %></td>
      <% if @book_detail.user == current_user %>
        <td>
          <%= link_to "Edit", edit_book_path(@book_detail) %>
        </td>
        <td>
          <%= link_to "Destroy", book_path(@book_detail), method: :delete, "data-confirm" => "本当に消しますか？" %>
        </td>
      <% end %>
    </tr>
  </table>
  </div>
 </div>
</div>
